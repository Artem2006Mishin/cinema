{
	"name": "movie-seat-server",
	"version": "1.0.0",
	"scripts": {
		"start": "json-server-auth db.json -r routes.json --host 0.0.0.0 --port $PORT"
	},
	"dependencies": {
		"json-server": "^0.17.4",
		"json-server-auth": "^2.1.0"
	}
}
